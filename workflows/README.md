# Workflows

Workflows are multi-step procedures that compose atomic skills. They own
sequencing, shared context, previews, confirmation payloads, and rollback.

Atomic capabilities remain in `skills/`. A future
`obsidian.organize_and_file` workflow will compose classification, tagging,
frontmatter generation, and note movement into one preview-first action.
