skill:
  name: core_text
  version: 0.1.0

capabilities:
  - text.classify
  - text.extract
  - text.compare
  - text.summarize

capability_metadata:
  text.classify:
    lane: small_model
    operation_type: semantic
    model_required: true
  text.extract:
    lane: small_model
    operation_type: semantic
    model_required: true
  text.compare:
    lane: small_model
    operation_type: semantic
    model_required: true
  text.summarize:
    lane: small_model
    operation_type: semantic
    model_required: true

permissions: {}
